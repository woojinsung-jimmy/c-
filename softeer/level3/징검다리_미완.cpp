#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

vector<int> bridge,each[100000001];

int main()
{
    int n, a,cur,cnt=1,b;
    cin>>n;

    for (int i = 0; i < n; i++)
    {
        cin>>a;
        bridge.push_back(a);
    }

    for (cur = 0; cur < bridge.size();b++) //3 1 4 2 1 4 1 5 1
    {
        for (int i = cur; i < ; i++)
        {
            /* code */
        }
        
    }
    printf("%d",cnt);
    

    
}

